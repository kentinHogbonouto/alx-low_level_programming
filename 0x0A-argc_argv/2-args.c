#include <stdio.h>

/**
 *main - function declaration
 *@argv: arrays of arguments
 *@argc: argument counter
 *Return: always 0
 */

int main(int argc, char **argv)
{
int i;
for(i = 0; i < argc; i++)
printf("%s\n", argv[i];
return (0);
}
